import { CommunicationFactory } from './communication-factory';

describe('CommunicationFactory', () => {
  it('should be defined', () => {
    expect(new CommunicationFactory()).toBeDefined();
  });
});
